package com.bnta.exercises.week_two_mon.arrays_exercise;

import java.util.Arrays;

public class Exercise5 {
    public static void main(String[] args) {

            // declare and initialise a String array which holds the values "a", "b", "c" and "d"
            // print the array to the console

//      the question requires us to declare and initialise, then doing so will look like this, seen below
//      the dimensions of the arrays are assigned inside curly brackets
        String[] letters = new String[]{"a", "b", "c", "d"};
        System.out.println(Arrays.toString(letters));
        }
}
